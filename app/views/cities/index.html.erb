<h1>Listing cities</h1>

<table>
  <thead>
    <tr>
      <th>Federal state</th>
      <th>Name</th>
      <th>Slug</th>
      <th>Zip code</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @cities.each do |city| %>
      <tr>
        <td><%= city.federal_state_id %></td>
        <td><%= city.name %></td>
        <td><%= city.slug %></td>
        <td><%= city.zip_code %></td>
        <td><%= link_to 'Show', city %></td>
        <td><%= link_to 'Edit', edit_city_path(city) %></td>
        <td><%= link_to 'Destroy', city, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New City', new_city_path %>
